{
  "buildCommand": "npm ci && npm run examples:install && npm run examples:build",
  "outputDirectory": "examples/dist",
  "framework": "vite",
  "devCommand": "npm run examples:dev",
  "ignoreCommand": "sh -c 'git rev-parse --verify HEAD^ >/dev/null 2>&1 || exit 1; git diff --quiet HEAD^ HEAD -- src examples package.json'"
}
